package com.example.arnolf.mapsmarkertest;

/**
 * Created by ArnOLF on 2015-07-20.
 */
public class TimeChecker {
/*
    //long startTime, elapsedTime;

    Markers markers;
    int markCount = 1;

    private static final ScheduledExecutorService worker =
            Executors.newSingleThreadScheduledExecutor();

    public TimeChecker(Markers markers) {
        this.markers = markers;
    }

    public void checkTime() {

        Runnable task = new Runnable() {
            @Override
            public void run() {
                delMarker("m" + markCount);
                markCount++;
            }
        };
        worker.schedule(task, 1, TimeUnit.MINUTES);
    }

    public void delMarker(String id) {

        markers.markerHashMap.get(id).remove();
        markers.markerHashMap.remove(id);
    }*/
}
